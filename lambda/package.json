{
  "name": "vehicle-status-lambda",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.js --bundle --minify --platform=node --target=node20 --outfile=../terraform/dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Antonio Estela",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/AntonioEstela/vehicle-status-monitoring/issues"
  },
  "homepage": "https://github.com/AntonioEstela/vehicle-status-monitoring#readme",
  "devDependencies": {
    "esbuild": "^0.27.0"
  }
}
